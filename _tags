true : bin_annot, package(bytes), warn(-40)

<src> : include
<test> : include

<src/*>: package(result), \
         package(ocplib-endian), \
         package(bigarray)

<test/*>: package(result), \
          package(ocplib-endian), \
          package(bigarray), \
          package(oUnit)
